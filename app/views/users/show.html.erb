
<nav class="navbar navbar-default">
<div class="container-fluid">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand" href="#"><%= @user.name %>'s Conclave</a>
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <ul class="nav navbar-nav">
      <li><%= link_to "Home", user_path(@user) %></li>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <li><%= link_to "Create course", new_course_path %> </li>
      <li><%= link_to "Find courses", courses_path %> </li>
      <li><%= link_to "Edit User", edit_user_path(@user) %></li>
      <li><%= link_to "Delete User", user_path(@user), data: {
          turbo_method: :delete,
          turbo_confirm: "Are you sure?"
        } %> 
      </li>
      <li><%= link_to "Logout", logout_path%> </li>
    </ul>
  </div><!-- /.navbar-collapse -->
</div><!-- /.container-fluid -->
</nav>
<div class='main container' style='text-align:left;'>
<div class='container'>
<h3> My Classes </h3>
</div>

<div class='list-group' >
<% if @courses.length() > 0 %>
<ul>
  <% @courses.each do |course| %>
    <button style="display:flex;" type="button" class="list-group-item list-group-item-action" aria-current="true">
      <%= link_to course.name, course %>
    </button>
  <% end %>
</ul>
<% else %>
  <div class='container'>
    <p> Looks like you haven't registered for any courses! Please click <%= link_to "here", courses_path %> to find a course to join. </p>
  </div>
<% end %>

</div>

<!-- 
TODO: When classes and posts are fixed will create a display for it
  -->

</div>
